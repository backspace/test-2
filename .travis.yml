language: shell

script: true

deploy:
  edge: true
  provider: script
  script: echo foo

after_deploy:
  - ls -al /tmp/travis
  - ls -al /tmp/travis/events
  - cat ~/.travis/agent
  - cat /tmp/travis/agent.log
