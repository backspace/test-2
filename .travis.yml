language: shell

before_script: cat ~/.travis/agent

script: true

deploy:
  edge: true
  provider: script
  script: echo foo

after_deploy:
  - ls -al /tmp/travis
  - ls -al /tmp/travis/events
  - cat /tmp/travis/agent.log
