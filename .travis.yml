language: php

php:
  - 7.0
  - 7.1
  - 7.2

stages:
  - name: quicktest
  - name: test

jobs:
  include:
    - stage: quicktest
      php: 7.3
      env: ONE=one
     
    - php: 7.2
      env: TWO=two

    - php: 5.4
      env: ONE=one

script: true
