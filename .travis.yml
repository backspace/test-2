language: php

php:
  - 7.0
  - 7.1
  - 7.2

env:
  - PHPCS_VERSION="dev-master" LINT=1
  - PHPCS_VERSION="3.0.2"

stages:
  - name: quicktest
  - name: test

jobs:
  include:
    - stage: quicktest
      php: 7.3
      env: PHPCS_VERSION="dev-master" LINT=1
     
    - php: 7.2
      env: PHPCS_VERSION="3.0.2"

    - php: 5.4
      env: PHPCS_VERSION="dev-master" LINT=1

script: true
