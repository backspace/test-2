language: php

php:
  - 7.0
  - 7.1

env:
  - ONE=one
  - TWO=two

stages:
  - name: quick
  - name: test

jobs:
  include:
    - stage: quick
      php: 7.3
      env: ONE=one
     
    - php: 7.2
      env: TWO=two

script: true
